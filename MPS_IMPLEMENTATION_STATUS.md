# MPS — سجل تنفيذ مواصفات 15 سبتمبر 2026

هذا السجل يقابل كل معرّف في `MPS_Store_Implementation_Brief`. يُحدَّث بعد كل
مجموعة عمل، لا في النهاية.

**الحالات:** `تم` · `موجود واجتاز` · `يحتاج إعداد مالك` · `تعذر التحقق` · `مؤجل`

**اكتمل الجدول:** ما بقى ولا بند بحالة «قيد التنفيذ». كل صف يحمل المسار الذي
تغيّر ودليلًا يقدر شخص آخر يعيد تشغيله — اسم اختبار أو أمر أو قياس — أو يقول
صراحة إنه إعداد مالك لا فجوة كود.

**قاعدة هذا الملف:** لا يُكتب «تم» لبند له واجهة فقط بلا تنفيذ، ولا «موجود
واجتاز» بلا دليل يمكن لشخص آخر إعادة تشغيله.

---

## ملاحظة على الميزات المؤجلة (§13 من المواصفات)

المواصفات تمنع **إضافة** قائمة رغبات أو مقارنة أو مراجعات عملاء بهذه الجولة.
الثلاثة **موجودة أصلاً** في المشروع قبل وصول هذه المواصفات (مراحل 5.7 و5.8
و5.9). فالمطلوب بحقها هو ما تقوله المواصفات نفسها: «إذا كانت موجودة بالفعل
تحقق أنها لا تنكسر فقط». لم يُضف لها شيء في هذه الجولة.

---

## الجدول

| المعرّف | الحالة           | المسارات المتأثرة                                                              | دليل التحقق                                                                                                                                                                                                                                                                                                                                | العائق                                                                                                                       |
| ------- | ---------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- |
| UI-01   | تم               | `app/[locale]/layout.tsx` · `app/globals.css`                                  | `tests/e2e/layout.spec.ts` → «Arabic pages resolve to the Arabic face». قياس قبل: أول خط بالسلسلة `Inter`؛ بعد: `IBM Plex Sans Arabic` على body وh1 والحقول والأزرار بالإدارة.                                                                                                                                                             | —                                                                                                                            |
| UI-02   | تم               | `app/globals.css` · `app/[locale]/(storefront)/page.tsx` · 14 حقل إدخال        | `tests/unit/contrast.test.ts` (11 حالة) + `tests/e2e/layout.spec.ts` → الـ Hero ≤56px على 1440 و28–36 على 390.                                                                                                                                                                                                                             | —                                                                                                                            |
| UI-03   | تم               | `components/layout/mobile-nav.tsx` · `app/globals.css`                         | حبس التركيز بالقائمة الجانبية وإرجاعه للزر، `scroll-padding` للهيدر وشريط الشراء، وعلامة ⚠ على كل `role="alert"`.                                                                                                                                                                                                                          | التباين وترتيب التركيز مفحوصان؛ **قارئ الشاشة لم يُجرَّب فعليًا** — مسجّل كفجوة.                                             |
| HOME-01 | تم               | `app/[locale]/(storefront)/page.tsx` · `lib/domain/price-bands.ts`             | ثلاث كروت «تسوّق حسب النوع» كانت تكرّر روابط الهيدر بحجم صورة hero وتدفع كل منتج تحت الطية على لابتوب. صارت chips، والعمود اللي فرغ يحمل شرائح ميزانية محسوبة من **أدنى وأعلى سعر فعلي** بنفس الدالة النقية اللي يستعملها `/guides` — روابط لفلاتر موجودة، لا محرّك توصيات.                                                                | —                                                                                                                            |
| HOME-02 | تم               | `page.tsx` · `server/queries/catalogue.ts`                                     | «الأكثر مبيعًا» كان يقرا `isBestSeller` — خانة تأشير تسويقية، وعنوان القسم ادّعاء عن شراء فعلي. صار يجمع أسطر الطلبات **المُسلَّمة** بـSQL خام (`OrderItem` يشير لنسخة وما يحمل `productId`)، وما يُرسم القسم أصلًا إذا ماكو تسليم — رفّ فارغ تحت ذاك العنوان اختلاق رقم.                                                                  | —                                                                                                                            |
| HOME-03 | تم               | `page.tsx`                                                                     | الشبكة كانت ستة أعمدة ثابتة فالبراند السابع يبقى لحاله بصف؛ صارت `auto-fit`. والرئيسية تعرض الماركات اللي عندها منشور فقط — اختصار لكتالوك فارغ طريق مسدود، و`/brands` يبقى الدليل الكامل.                                                                                                                                                 | —                                                                                                                            |
| HOME-04 | تم               | `components/layout/language-switcher.tsx` · `site-header.tsx`                  | مبدّل اللغة كان يعرض **حرفًا عربيًا واحدًا**، وهو تخمين لمن ما يقرا العربية. صار الإملاءان موجودين خلف breakpoint كعنصرين لا كسلسلة أصناف — `hidden` ملحقة بقالب فيه أداة عرض ما تخفي شيئًا (حارس معماري يمنعها الآن).                                                                                                                     | —                                                                                                                            |
| HOME-05 | تم               | `page.tsx` · `site-footer.tsx`                                                 | شريط الثقة كان يؤكد ثلاثة أشياء بلا مكان للتحقق؛ صار كل عنصر يربط بالصفحة اللي تقول عنه شيئًا. والـCTA الأخير يشير للأدلة والتواصل بدل تكرار زر التصفح بعد ثلاثة رفوف.                                                                                                                                                                     | صفحات الضمان والإرجاع والخصوصية والشروط **لم تُكتب عمدًا** — نصّها شروط تجارية يضعها المالك (§13.13)، لا فجوة كود.           |
| CAT-01  | تم               | `product-card.tsx` · `products/page.tsx`                                       | الصورة `contain` بدل `cover`؛ السعر من أرخص نسخة **قابلة للشراء**؛ «يبدأ من» عند اختلاف الأسعار؛ 4 أعمدة داخل الفلاتر. `check:layout` بعدها: كل الكروت 262px.                                                                                                                                                                              | —                                                                                                                            |
| CAT-02  | تم               | `filter-panel.tsx`                                                             | فلتر السعر أُضيف **أولًا** قبل RAM. فلاتر RAM/التخزين كانت أصلًا مشروطة بوجود الـfacet فما تظهر للإكسسوارات.                                                                                                                                                                                                                               | —                                                                                                                            |
| CAT-03  | تم               | `catalogue-toolbar.tsx` · `tests/e2e/catalogue.spec.ts`                        | 3 اختبارات متصفح: المدى السعري ينزّل العدد ويرجع بالـback، أي فلتر يرجّع للصفحة 1، والـdrawer يضل مفتوح ويغلق بزر «عرض N نتيجة».                                                                                                                                                                                                           | الاقتراحات الفورية غير موجودة أصلًا فالـdebounce غير منطبق — مسجّل لا مدّعى.                                                 |
| CAT-04  | تم               | `offers/page.tsx`                                                              | «عرض الكل» كان يؤدي لنفس المنتجات؛ صار «فلترة العروض».                                                                                                                                                                                                                                                                                     | —                                                                                                                            |
| PDP-01  | تم               | `features/product/components/product-purchase.tsx` · `delivery-estimate.tsx`   | الصندوق جنب أزرار الشراء كان يحمل **تصنيف المنتج** — اللي يقوله breadcrumb بسطرين فوق ولا أحد يقرر شراء عليه — بينما أجرة التوصيل أول ما تظهر بصفحة الدفع بعد العنوان. صار الضمان والتوصيل سوا، والتقدير quote حقيقي من `quoteDeliveryFor` نفسها اللي تسعّر الطلب.                                                                         | —                                                                                                                            |
| PDP-02  | تم               | `variant-picker.tsx` · `product-purchase.tsx`                                  | دائرة اللون ما تسمّي شيئًا: صار عنوان المجموعة يحمل القيمة المختارة. والتركيبة غير المتاحة مشطوبة ومُعلنة «نفد» لا مجرد باهتة — 40% شفافية تُقرأ كزر ضعيف، ولا تُقرأ إطلاقًا لمن ما يميّز الفرق. والاختيار صار **واحدًا** يتشاركه المنتقي وشريط الهاتف.                                                                                    | —                                                                                                                            |
| PDP-03  | موجود واجتاز     | `product-gallery.tsx`                                                          | المعرض موجود ويجمع الصور والفيديو بشريط واحد، والمشغّل يُنشأ عند الضغط فقط. المصغّرات كانت بلا اسم يُقرأ — أُصلح بجولة الوصولية السابقة. لم يُغيَّر شيء بهذه الجولة.                                                                                                                                                                       | —                                                                                                                            |
| PDP-04  | تم               | `products/[slug]/page.tsx` · `features/product/components/product-content.tsx` | المواصفات كانت بعمود 380px جنب النص، فجدول بعشرين صفًا يصير ضعف طوله والمحتوى جنبه ينتهي بنص الطريق. المحتوى صار بعرض كامل والمواصفات تليه، مجموعتان بالعرض حيث يسمح المكان وواحدة على الهاتف. والصفحة تعرض فقط المواصفات اللي **نوعها** ما زال يعلنها.                                                                                    | —                                                                                                                            |
| PDP-05  | تم               | `mobile-buy-bar.tsx` · `product-purchase.tsx`                                  | الشريط كان يضيف `variants[0]` المسمّى `cheapest` رغم أن الاستعلام يرتّب بـ`sortOrder` — فمن يختار 256GB أزرق يضيف 128GB أسود بسعر ثاني. صار الاختيار واحدًا. وكذلك حشوة `env(safe-area-inset-bottom)` حتى يتخطى مؤشر الهوم بهاتف بنتوء. `tests/e2e/buy-bar.spec.ts` يسقط على السلك القديم بالسعرين اللي لكاهم.                             | —                                                                                                                            |
| FLOW-01 | تم               | `schemas/address.ts` · `server/services/account.ts` · `features/account/`      | `tests/integration/account.test.ts` (6): الملف يكتب عمودين فقط ولا يمس `role`/`isActive`، والعنوان صف واحد لكل زبون مهما تكرر الحفظ. مُثبت بكسر متعمّد: تحويل الحفظ إلى create دائمًا أسقط 3 اختبارات.                                                                                                                                     | تغيير البريد غير منفّذ عمدًا — يحتاج تأكيدًا على العنوان القديم، والبريد غير مهيّأ (§7).                                     |
| FLOW-02 | موجود واجتاز     | `server/queries/cart.ts` · `app/[locale]/(storefront)/cart/page.tsx`           | السلة تقرأ السعر من صف الـvariant في كل قراءة وتحسب `availability` لكل سطر؛ `hasBlockedLine` يعطّل زر الإتمام ويظهر سبب المنع. لم يُغيَّر شيء.                                                                                                                                                                                             | —                                                                                                                            |
| FLOW-03 | تم               | `features/checkout/actions.ts` · `checkout-form.tsx` · `checkout/page.tsx`     | المدخلات ترجع مع الرفض (`CheckoutState.values`) فما تضيع الحقول الستة بخطأ رقم واحد؛ والعنوان المحفوظ يملأ النموذج مع quote محسوب بالسيرفر. `tests/e2e/checkout-recovery.spec.ts`.                                                                                                                                                         | —                                                                                                                            |
| FLOW-04 | موجود واجتاز     | `server/queries/order.ts` · `lib/domain/order-grant.ts`                        | Guest Order Grant (32 بايت CSPRNG، يُخزَّن هاشه فقط) نُفّذ بالدفعة A؛ `tests/e2e/order-privacy.spec.ts` يثبت أن رقم الطلب وحده لا يفتح طلب غيرك.                                                                                                                                                                                           | —                                                                                                                            |
| ADM-01  | تم               | `features/admin/components/admin-nav.tsx` · `admin-shell.tsx`                  | 13 رابطًا بلا أي علامة على الصفحة الحالية — ولا `aria-current` ولا خلفية. صار الرابط الحالي ملوّنًا ويحمل `aria-current="page"`، و`/admin` يطابق تمامًا بينما البقية تطابق شجرتها حتى يضيء «المنتجات» داخل `/admin/products/new`. عرض الشريط 208px ← 224px.                                                                                | —                                                                                                                            |
| ADM-02  | تم               | `product-form.tsx` · `components/ui/use-unsaved-warning.ts`                    | تحذير المغادرة عند وجود تعديلات غير محفوظة. يغطي إعادة التحميل وإغلاق اللسان؛ لا يغطي التنقل داخل التطبيق — App Router ما يوفّر منعًا مدعومًا، وهذا مكتوب بالملف بدل ادّعائه. الأقسام والشريط الثابت و`pb-24` موجودة أصلًا.                                                                                                                | تخزين مسودة النموذج بالمتصفح مرفوض عمدًا (الخطة تمنعه).                                                                      |
| ADM-03  | موجود واجتاز     | `server/services/admin-products.ts` · `lib/domain/product.ts`                  | مصدر الحقيقة مفصول أصلًا: المواصفات على `ProductAttributeValue` والنسخ على `ProductVariant`. تبديل نوع المنتج لا يمسح القيم — `tests/integration/admin-products.test.ts:394` و`admin-taxonomy.test.ts:494` يثبتان أن فكّ الربط يخفي ويعيد بلا فقد. لم يُغيَّر شيء.                                                                         | —                                                                                                                            |
| ADM-04  | تم               | `product-media-editor.tsx`                                                     | معاينة مصغّرة لكل صورة وشارة «الصورة الرئيسية» على الأولى. الرفع والترتيب والحذف موجودة أصلًا. `<img>` عادية لا `next/image` عمدًا: الرابط أيًا كان، و`next/image` يرمي خطأً على host خارج `remotePatterns` فيسقط النموذج كله.                                                                                                             | —                                                                                                                            |
| ADM-05  | تم               | `lib/datetime.ts` + 7 صفحات إدارة + 4 واجهة متجر                               | ثلاث شاشات إدارة كانت تنسّق التاريخ بلا منطقة زمنية وبأرقام عربية-هندية بينما البقية ببغداد وأرقام لاتينية. صار الكل عبر وحدة واحدة مُختبَرة (4 اختبارات)، وحارس يمنع أي `Intl.DateTimeFormat` خارجها — مُثبت بإرجاع نسخة يدوية وسقوط الحارس باسم الملف والسطر.                                                                            | —                                                                                                                            |
| ADM-06  | موجود واجتاز     | `server/queries/admin.ts` · `app/[locale]/(admin)/admin/orders/`               | الجدول يحمل الرقم والوقت والعميل والمحافظة والحالة والإجمالي؛ الخط الزمني يعرض من نفّذ الانتقال (`actorId` مقروء باسمه)؛ الأزرار تأتي من آلة الحالة فالقفزة غير المسموحة غير معروضة؛ ملاحظة الموظف الداخلية لا يقرأها استعلام الزبون (`server/queries/order.ts` يختار `toStatus` و`createdAt` فقط).                                        | —                                                                                                                            |
| ADM-07  | تم               | `schemas/admin.ts` · `schemas/blank.ts` · `delivery-rates-table.tsx`           | أخطر ما وُجد بهذه المجموعة: خانة أجرة فارغة كانت تُحفظ 0 = توصيل مجاني لتلك المحافظة على كل طلب بعدها. `blankIsMissing` صار مشتركًا ويغطي الأجرة وطرفَي المدى والأجرة الافتراضية للمتجر. 7 اختبارات وحدة؛ 3 منها تسقط لحظة نزع الغلاف. مؤشّر «غير محفوظ» لكل صف، وزر الحفظ معطّل ما لم يتغيّر الصف، والأجرة الافتراضية placeholder.        | —                                                                                                                            |
| SEC-01  | موجود واجتاز     | `server/auth/guards.ts` · كل `server/services/admin-*.ts`                      | `requireStaff()`/`requireAdmin()` داخل كل تصدير إداري، والحارس المعماري يرفض `requireUser()` هناك (كان يقبله — القاعدة هي اللي كانت غلط لا الكود). الدور و`isActive` يُقرآن من **الصف** لا من الجلسة، لأن `cookieCache` خمس دقائق كان يبقي موظفًا مفصولًا داخل اللوحة.                                                                     | —                                                                                                                            |
| SEC-02  | موجود واجتاز     | `server/queries/order.ts` · `lib/domain/order-grant.ts`                        | الوصول للطلب = جلسة تملكه، أو تفويض ضيف 32 بايت CSPRNG يُخزَّن **هاشه** فقط ويُطابَق داخل SQL بلا مقارنة نصية. الكوكي القديم اللي كان يحمل رقم الطلب نفسه أُزيل — كان مفتاحًا شغّالًا لأي طلب. `tests/e2e/order-privacy.spec.ts` + `tests/integration/order-access.test.ts`.                                                               | —                                                                                                                            |
| SEC-03  | موجود واجتاز     | `server/cookies.ts` · `server/auth/auth.ts`                                    | كل كوكي عبر `appCookieOptions()` و`secure` يتبع **مخطط** `BETTER_AUTH_URL` لا `NODE_ENV` (حارس يمنع الرجوع)، httpOnly وsameSite=lax، جلسات بقاعدة البيانات 30 يومًا، وتسجيل الخروج يمسح `mps.order_grant` كذلك.                                                                                                                            | كلمة مرور حساب الـDemo ما زالت الافتراضية الضعيفة — `db:seed` يرفض الإنتاج، ومسجّلة كإعداد مالك قبل الإطلاق.                 |
| SEC-04  | موجود واجتاز     | `server/rate-limit.ts` · `server/auth/auth.ts`                                 | حدود better-auth على `/api/auth/*` (الدخول والتسجيل والاسترجاع كلها تمر بالموجّه HTTP لا باستدعاء مباشر، وإلا ما تُطبَّق)، وتتبّع الطلب له حدّ خاص بالهاتف لا الـIP. رسالة واحدة لكل حالات الرفض فما تكشف وجود حساب.                                                                                                                       | العدّاد داخل العملية: ما ينجو من إعادة تشغيل ولا يُشارَك بين نسختين — مسجّل بـ§15 وجدول العدّاد هو الحل يوم تصير نسخة ثانية. |
| SEC-05  | موجود واجتاز     | `schemas/*` · `lib/safe-redirect.ts` · Next Server Actions                     | كل مدخل يمر بـZod على السيرفر. Next يتحقق من Origin مقابل Host لكل Server Action افتراضيًا ولا `allowedOrigins` موسّع. Prisma يبني الاستعلامات مُعلَّمة، وSQL الخام الوحيد (أفضل المبيعات، أقفال الصفوف) يستعمل template tags. ماكو HTML غني من مستخدم أصلًا — نص المراجعات والأدلة يُرسم بـReact بلا مسار إلى markup.                     | —                                                                                                                            |
| SEC-06  | موجود واجتاز     | `server/services/order.ts` · `lib/domain/coupon.ts`                            | العميل يرسل `variantId` والكمية والكود فقط؛ ماكو مكان بالـschema لمبلغ. كل رقم يُعاد حسابه داخل نفس الـtransaction من صفوف الـvariant و`DeliveryRate` و`Coupon`. `tests/e2e/coupon.spec.ts` يقارن ما عُرض بما شُحن فعلًا من Postgres.                                                                                                      | —                                                                                                                            |
| SEC-07  | موجود واجتاز     | `server/services/order.ts` · `lib/domain/checkout-request.ts`                  | قفل صف على السلة (`FOR UPDATE`) + مفتاح idempotency = هاش(معرّف السلة + UUID من المتصفح) على فهرس فريد، فالمحاولة الثانية تُسلَّم الطلب الأول لا طلبًا ثانيًا. حجز المخزون UPDATE مشروط ذرّي. اختبارات تكامل تُسقط النسخة الساذجة (تبيع القطعة الأخيرة مرتين).                                                                             | —                                                                                                                            |
| SEC-08  | موجود واجتاز     | `server/services/admin-orders.ts` · `lib/domain/order-state.ts`                | الانتقال compare-and-set (`updateMany where {id, status}` + `count === 1`) فما يصير حدثان ولا إطلاق حجز مرتين. `OrderItem` يحفظ السعر والاسم والـSKU ولقب النسخة، والأجرة محفوظة على الطلب. ماكو TTL مخترع للحجز.                                                                                                                          | —                                                                                                                            |
| SEC-09  | موجود واجتاز     | `server/services/admin-storage.ts` · `lib/domain/image-file.ts`                | `requireStaff()` **قبل** قراءة الجسم، والنوع يُقرَّر من بايتات الملف لا من الامتداد (SVG مُعاد التسمية مرفوض بأربع طرق تمويه مُختبَرة)، والاسم 16 بايت عشوائية والمجلد slug المنتج — فما يُعاد استعمال أي شيء اختاره الرافع. حد حجم وحد أبعاد (16px إلى 100 مليون بكسل).                                                                   | —                                                                                                                            |
| SEC-10  | موجود واجتاز     | `config/env.ts` · `server/services/admin-storage.ts`                           | ماكو مفتاح سري بمتغير `NEXT_PUBLIC_*`؛ `SUPABASE_SERVICE_ROLE_KEY` يُقرأ بوحدة `server-only` واحدة. صفحات الحساب والطلب والسلة ديناميكية و`noindex`. السجلات تطبع كائن الخطأ فقط — ولا سطر يطبع هاتفًا أو عنوانًا.                                                                                                                         | RLS ومفاتيح المشروع خارج المستودع — إعداد مالك (§18).                                                                        |
| SEC-11  | موجود واجتاز     | `lib/security-headers.ts` · `tests/e2e/security.spec.ts`                       | CSP وHSTS مبنيان بوحدة مُختبَرة، والـe2e يحمّل كل صفحة ومعها مستمع `securitypolicyviolation` ويسقط على أي خرق غير خرق Zod المعروف. الاختبار يجري على **build إنتاج** لا على dev.                                                                                                                                                           | النشر نفسه (HTTPS والدومين) إعداد مالك.                                                                                      |
| SEC-12  | يحتاج إعداد مالك | —                                                                              | ماكو آلية backup داخل المستودع، وماكو وصول لقاعدة إنتاج من هنا. **لم يُدَّعَ شيء.** الاسترجاع يُجرَّب على قاعدة اختبار يوم يصير عندها DATABASE_URL إنتاجية.                                                                                                                                                                                | يحتاج قاعدة إنتاج ومزوّدًا — بند مالك صريح.                                                                                  |
| OPS-01  | موجود واجتاز     | `tests/e2e/performance.spec.ts`                                                | القياس مسجّل بـ§14 و§15 بالأرقام: الكتالوك 12 استعلامًا بـ4.8ms من استجابة 34ms، والمتصفح كان ينزّل 1048kB صار 670kB. ميزانية JS بالـe2e تمنع الرجوع. **لم تُدّعَ أرقام حقلية (LCP/INP/CLS) — القياس المختبري مؤشر لا إثبات.**                                                                                                             | أرقام الحقل تحتاج زيارات حقيقية على دومين — بند مالك.                                                                        |
| OPS-02  | تم               | `tests/e2e/layout.spec.ts`                                                     | فحص الانزلاق الأفقي توسّع من عرضين إلى **ستة**: 360 و390 و768 و1366 و1440 و1920. 360 هو أضيق أندرويد شائع بالعراق وهو أول مكان يظهر بيه الفائض.                                                                                                                                                                                            | —                                                                                                                            |
| OPS-03  | موجود واجتاز     | `app/[locale]/layout.tsx` · `products/[slug]/page.tsx` · `app/sitemap.ts`      | عنوان ووصف لكل منتج، canonical وhreflang ar/en على slug واحد، `metadataBase` فيصير كل رابط OG مطلقًا، sitemap للمنشور فقط، وصفحات الحساب والسلة والطلب والمقارنة `noindex`. JSON-LD يعكس السعر والتوفر الفعليين وماكو مراجعات مختلقة.                                                                                                      | ماكو صورة OG عامة للصفحات غير المنتجات — قرار هوية بصرية للمالك لا فجوة كود.                                                 |
| OPS-04  | تم               | `lib/domain/analytics.ts` · `features/analytics/`                              | أربع نقاط أحداث (`view_item`/`add_to_cart`/`begin_checkout`/`purchase`) تُدفع لـ`window.dataLayer` — بلا مزوّد وبلا طلب خارجي وبلا أي بيانات شخصية. الشراء يُحسب **مرة واحدة**: `?placed=1` أولًا، ثم رقم الطلب بـ`sessionStorage` ثانيًا. 5 اختبارات وحدة + `tests/e2e/analytics.spec.ts` يعيد تحميل صفحة التأكيد ويتأكد ما انباعت مرتين. | ربط مزوّد فعلي (GTM أو غيره) إعداد مالك.                                                                                     |

---

## التفاصيل

### المجموعة أ — الأساس المشترك

**UI-01 — العربية كانت تُرسم بـ Arial، وهذا مقاس لا مستنتج.**

`next/font` يحشر وجهًا ثانيًا اسمه `"Inter Fallback"` داخل `--font-latin`،
مصدره `local("Arial")`. والسلسلة كانت:

```
Inter, "Inter Fallback", "IBM Plex Sans Arabic", …
```

**وArial بيه حروف عربية.** يعني على كل جهاز عنده Arial — كل ويندوز وكل ماك،
ومنها جهاز المالك — كل حرف عربي بالمتجر كان يُرسم بـArial، وIBM Plex Sans
Arabic ما وصلته النوبة أبدًا. هذا بالضبط «طابع مختلف أقرب لخط افتراضي».

المفتاح الموثّق لإطفاء ذاك الوجه (`adjustFontFallback: false`) **ما أثّر**:
هذا البناء يمر بـTurbopack. مُثبت بضبطه، ومسح `.next/cache`، وإعادة البناء،
وطلوع نفس الـ content hash. فالحل بالـCSS: الوجه العربي **أولًا** بصفحات
العربية، وما يقدر أي bundler يوقفه بصمت.

| السطح                              | قبل   | بعد                  |
| ---------------------------------- | ----- | -------------------- |
| `/ar` body · h1 · button           | Inter | IBM Plex Sans Arabic |
| `/ar/admin` input · select · label | Inter | IBM Plex Sans Arabic |
| `/en` body                         | Inter | Inter (بلا تغيير)    |

**UI-02 — رقمان ما كانا يجتازان الحد، وماكو شي يكَول.**

| الرمز             | قبل       | التباين      | بعد                              | التباين  |
| ----------------- | --------- | ------------ | -------------------------------- | -------- |
| `--color-subtle`  | `#9ca3af` | **2.43:1** ✗ | `#6b7280`                        | 4.63:1 ✓ |
| `--color-success` | `#0f8a4a` | **4.23:1** ✗ | `#0d7a41`                        | 5.19:1 ✓ |
| حدود الحقول       | `#e7e7e9` | **1.23:1** ✗ | `--color-border-field` `#8e8e96` | 3.11:1 ✓ |

`--color-subtle` هو اسم البراند على كل كارت منتج، وكل تاريخ، وكل SKU.
و`--color-success` هو سطر «متوفر». وحدود الحقول هي الشي الوحيد اللي يكَول
للزبون وين يكتب (WCAG 1.4.11 يطلب 3:1).

`tests/unit/contrast.test.ts` يقرا الرموز من `globals.css` نفسه ويعيد الحساب،
فما يقدر رمز يتغيّر هناك بلا ما ينقاس هنا.

كذلك: السلم اللوني صار 4 درجات كلها تجتاز، الحاوية 1440px، الحواف موحّدة 8px،
وارتفاع السطر 1.6 (العربية 1.7).

**UI-03 — `aria-modal` كانت تكذب.**

القائمة الجانبية بالهاتف تحمل `aria-modal="true"` — يعني تكَول لقارئ الشاشة إن
كل شي وراها غير موجود — **بينما Tab كان يطلع منها للصفحة اللي وراها**. هسه
التركيز ينحبس داخلها، يدخلها عند الفتح، ويرجع لزر الفتح عند الغلق.

وكذلك: `scroll-padding` حتى العنصر المركَّز ما ينخفي تحت الهيدر الثابت أو شريط
الشراء، وعلامة ⚠ على كل `role="alert"` (30 موضع) بقاعدة CSS وحدة — لأن رسالة
خطأ بالأحمر وبس ما تنقرا لمن عنده عمى ألوان (WCAG 1.4.1).

### المجموعة ب — الكارت والفلاتر والعروض

**فلتر السعر كان محسوبًا ومخفيًا.** `getCatalogueFacets` يحسب `priceRange` من
يوم بُني الكتالوك، و`min`/`max` موجودين بعقد الـURL ومُتحقَّق منهم — **وماكو ولا
عنصر يعرضهم**. يعني الزبون ما يكدر يفلتر بالسعر إلا إذا كتبه بشريط العنوان.
أُضيف **أول الفلاتر** قبل RAM، لأن الميزانية أول سؤال يسأله مشتري تلفون.

**والاختبار لكَه باگ ثاني بنفس الفلتر:** حطّيت `min`/`max` على الحقول بحدود مدى
الأسعار — وهذا **يمنع إرسال الفورم صامتًا**: HTML5 يرفض رقمًا تحت الحد، فالزبون
اللي يكتب 100000 والأرخص 120000 ما يصير شي غير فقاعة تحذير. الحدود انشالت،
والمدى صار تلميحًا بالـplaceholder، والتقييد بقى بالسيرفر حيث كان أصلًا.

**الصورة كانت تُقص.** `object-cover` يقص أطراف المنتج — واضح على الإكسسوارات
والعلب العريضة. صارت `object-contain` مع حشوة.

**السعر كان يمكن يكون طُعمًا.** الكارت كان ياخذ أرخص نسخة **فعّالة**؛ صار ياخذ
أرخص نسخة **قابلة للشراء**، وإلا يعرض سعرًا ما يكدر أحد يشتريه. و«يبدأ من»
تظهر بس لما الأسعار تختلف فعلًا.

**الأعمدة:** 5 → **4** داخل القائمة اللي بيها فلاتر. الرفوف بالرئيسية تبقى 5،
لأن ماكو sidebar ياخذ من عرضها.

**الـdrawer بالهاتف كان يسكّر على أول ضغطة.** تختار براند → ينغلق → تفتحه →
تختار ذاكرة → ينغلق. هسه يضل مفتوح وزر **«عرض N نتيجة»** هو اللي يرجّعك.
وينحبس التركيز بيه مثل قائمة التنقل — عبر `useFocusTrap`، **نسخة واحدة**
يستعملها الاثنان.
